
CMAKE_MINIMUM_REQUIRED(VERSION 2.4)

PROJECT(elfhacks)

SET(ELFHACKS_SOVER 0)
SET(ELFHACKS_VER 0.4.1)

IF (NOT CMAKE_BUILD_TYPE)
  SET(CMAKE_BUILD_TYPE "Release")
ENDIF (NOT CMAKE_BUILD_TYPE)

SUBDIRS(src)
SUBDIRS(examples)
